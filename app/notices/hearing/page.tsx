import Notice from "@/components/ui/notices/hearings/custom";

export default async function Data({ listId }: { listId: number }) {
  return (
    <div className="">
      <Notice />;
    </div>
  );
}
